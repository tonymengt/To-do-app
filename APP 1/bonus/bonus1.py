# text = input("enter some text: ")
# length = len(text)
# print(len(text))

import random

lower = input('enter the lower bound: ')
upper = input('enter the upper bouond: ')

print(random.randint(int(lower), int(upper)))
